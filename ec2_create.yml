
- name: Create AWS resources
  sudo: yes
  hosts: 
    - all
  roles:
    - { role: ec2_ubuntu_enableswap, tags: swap }
    - { role: accounts, tags: accounts }
    - { role: docker, tags: docker }
    - { role: rancher, tags: rancher }
    - { role: securesites, tags: securesites }
